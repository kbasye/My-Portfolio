<h1>Work</h1>
<% @projects.each do |project| %>
	<h2><%=link_to project.title, project_path(project) %></h2>
	<p><%= project.caption.html_safe %></p>
	<ul>
	<% project.tags.each do |tag| %>
		<li><%= link_to tag.name, tagged_path(tag) %></li>
	<% end %>
	</ul>
<% end %>